<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".CARMAApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CARMA_android_app"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- ContextInputActivity - LAUNCHER: user sets context before preview -->
        <activity
            android:name=".ContextInputActivity"
            android:exported="true"
            android:label="CARMA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PreviewActivity - shown after context input + API call -->
        <activity
            android:name=".PreviewActivity"
            android:exported="false"
            android:label="CARMA Preview" />

        <!-- MainActivity - Available but not launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="Home" />

    </application>

</manifest>